spring:
  application:
    name: registry

server:
  port: ${PORT:8761}

eureka:
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
  client:
    registerWithEureka: false
    fetchRegistry: false

  server:
    waitTimeInMsWhenSyncEmpty: 0


